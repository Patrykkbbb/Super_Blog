<!DOCTYPE html>
<html>
  <head>
    
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <div class="container">
    <div class="langs">
      <%= link_to "🇬🇧", url_for(locale: :en) %>
      <%= link_to "🇵🇱", url_for(locale: :pl) %>
    </div>
 <div class="logo"><a href="<%= root_path %>"><%= image_tag "/assets/logo.png" %></a></div>
    <br>
    <hr>
    <br>
    <% if user_signed_in? %>
      <% if current_user.admin? %>
        <%
=begin%>
 <%= link_to "Panel administratora", admin_dashboard_path %> 
<%
=end%>
      <% end %>
        <% if current_user.admin? %>
          <p> <%= t "application.body.logged_as"%>: <b> <%= current_user.nickname %> (Administrator) </b></p>
        <% else %>
          <p> <%= t "application.body.logged_as"%>: <b> <%= current_user.nickname %> </b></p>
        <% end %>
      <%= link_to (t ("application.body.profile")), user_path(current_user.id) %>
      <%= link_to (t ("application.body.edit")), edit_user_registration_path %>
      <%= button_to (t ("application.body.logout")), destroy_user_session_path, :method => :delete %>
    <% else %>
      <%= link_to (t ("application.body.login")), new_user_session_path %>
      <%= link_to (t ("application.body.signin")), new_user_registration_path %>
    <% end %>
    <% if notice %>
      <div class="alert alert-success" role="alert"><%= notice %></div>
    <% elsif alert %>
    <div class="alert alert-danger" role="alert"><%= alert %></div>
    <% end %>

    <%= render "search/form" %>
    <%= yield %>

</div>
  </body>
</html>
