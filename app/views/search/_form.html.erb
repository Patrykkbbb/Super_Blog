<%= search_form_for(@query, url: search_path, method: :get) do |f| %>
    <%= f.search_field :title_i_cont_any, placeholder: "#{t "search.form.placeholder"}" %>
    <%= f.submit "#{t "search.form.button"}" %>
<% end %>