<%= form_with model: @blog_post.comments.build do |form| %>
    <div>
      <%= form.hidden_field :user_id, value: "#{current_user.id}" %>
    </div>
    <div>
      <%= form.label :body %>
      <%= form.text_area :body %>
    </div>
    <%= form.submit "Komentuj" %>
<% end %>